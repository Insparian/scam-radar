# Scam Radar worker

This package contains the short-lived, fixture-first batch pipeline. Live HTTP,
Gemini, and production Supabase transports are intentionally absent until the
external-activation checkpoint is approved.
